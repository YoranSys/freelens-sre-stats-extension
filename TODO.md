# SRE Stats Extension TODO

## Next features

- Add time range filters for events (last 1h, 6h, 24h, 7d).
- Display node pressure conditions with counts per condition.
- Show top namespaces by warning events and pod issues.
- Add pod restart statistics (top 10 by restart count).
- Add per-node taint and cordon details drill-down.
- Show resource usage heatmaps (CPU/Memory) using metrics APIs.
- Export summary to CSV/JSON for incident reports.
- Add preferences for thresholds (warning > N, pod issues > N).
